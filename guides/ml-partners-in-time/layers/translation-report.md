# ML PiT translation report — legacy build → schema v0

Generated by `app/scripts/translateMlpit.ts` from
`ml-partners-in-time/build/guide.json` (last committed 2026-06-07).
Mechanical translation: no content invented (§24, §0.2).

## Counts

- Chapters: 9 route chapters (legacy roundup chapters became checklist widgets)
- Steps: 139
- Checklist widgets: 2 (12 rows)
- RA mapping entries: 55 of 55 achievements (all mapped)
- Images copied: 12

## Flagged items (confidence "flagged")

- ml-partners-in-time:c2:s17 (ach-99789 "Harvest Bean: Bowser's Castle"): The route source marks bean spots inline with the word 'BEAN', but its bean markers only begin from the Vim Factory onward — it does not enumerate the 8 individual dig locations inside Bowser's Castle. A bean-location source or map is needed to pin each spot.

## Translation notes

- Step ml-partners-in-time:c5:s12 anchors more than one missable achievement; only the first callout ("Only attack Petey while he's grounded — never while he's flying or diving.") became the step's missable deadline.
- Step ml-partners-in-time:c9:s5 anchors more than one missable achievement; only the first callout ("Win the saucer dogfight against the Mother Ship without being hit once.") became the step's missable deadline.
- Roundup intro for "Roundup — Bro-Item Masters" has no home on a checklist widget and was dropped: "Ten challenge achievements for landing a set number of hits with each Bro Item in a single attack. Best farmed against a single tough enemy or boss so every hit lands on one target. Keep a stock of each item (most are common in late-game blocks; Red/Copy/Mix Flowers show up from Toad Town onward)."
- Roundup intro for "Roundup — 100% Completion" has no home on a checklist widget and was dropped: "The two game-spanning completion achievements. Both are missable: dig every bean as you go (the route flags each 'BEAN' inline and each region has its own Harvest Bean achievement), and never sell the item-block-only pants."

## Recorded losses (content with no schema v0 home)

- Achievement metadata bodies: `description`, `howto`, `points`,
  `rarity`, `badge_img`, `type_label` — RA metadata comes from the RA
  API at sync/compile time; the hand-written `howto` prose remains in the
  legacy file, which stays in the repo until the Phase 5 formal recompile.
- `callout_do` for step-anchored achievements (kept only where it became a
  missable deadline or a checklist row note).
- `missable` on the two roundup completion achievements (checklist rows
  carry no missable flag; their notes state it).
- `game.accent`, `game.spoiler_warning`, chapter `kind`, per-source
  `note` fields.
- Legacy `game.slug` was "ml-partners-in-time-ra"; the canonical slug is
  the existing folder name "ml-partners-in-time" (§20.3).
- Library `title`/`game`/`platform` were normalized from the legacy
  display strings ("… — Achievement Guide", "Nintendo DS · RetroAchievements").
